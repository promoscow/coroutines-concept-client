spring:
  profiles:
    default: docker
  application:
    name: coroutines-concept-client
server:
  port: 8001
app:
  service-prefix: client
  requests-per-second: 10
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true